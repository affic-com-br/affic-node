## What changed

<!-- One or two sentences. Link the issue if there is one. -->

## Why

<!-- The problem this solves. -->

## Checklist

- [ ] `npm run lint`, `npm run typecheck`, and `npm test` pass
- [ ] Tests cover the new behaviour (including the failure paths)
- [ ] A changeset is included (`npx changeset`), or this PR changes nothing consumers can see
- [ ] Public API changes are reflected in the README and in the TSDoc comments
- [ ] No new runtime dependency (the SDK ships zero; open an issue first if you need one)
